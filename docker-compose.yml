version: '3.9'

services:

  postgres:
    image: postgres:15
    container_name: labcontrol_db
    restart: always
    environment:
      POSTGRES_DB: labcontroldb
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"